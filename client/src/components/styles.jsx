import { makeStyles } from '@material-ui/core/styles.jsx';

const useStyles = makeStyles((theme) => ({
  container: {
    backgroundColor: theme.palette.background.paper,
  }
}));

export default useStyles;
